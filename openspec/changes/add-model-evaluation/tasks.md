## 1. Evaluation Script
- [x] Implement `models/evaluate_models.py` that loads TF-IDF features, splits a test set deterministically, and evaluates all available models (SVM, Logistic Regression).
- [x] Produce human-readable comparisons (accuracy, precision/recall/F1 per class) and exit cleanly when a model artifact is missing.

## 2. Documentation & Validation
- [x] Document how to run the evaluation script and interpret its output in the README or a dedicated section.
- [x] Run the script to confirm it works with the current artifacts, capturing output for verification.
